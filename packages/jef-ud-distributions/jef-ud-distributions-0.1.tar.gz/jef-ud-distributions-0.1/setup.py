from setuptools import setup

setup(name='jef-ud-distributions',
      version='0.1',
      description='Gaussian and Binominal distributions',
      packages=['jef-ud-distributions'],
      zip_safe=False)
