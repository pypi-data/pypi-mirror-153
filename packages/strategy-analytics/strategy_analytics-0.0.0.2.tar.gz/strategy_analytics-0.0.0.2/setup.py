from setuptools import find_packages, setup
setup(
    name='strategy_analytics',
    packages=find_packages(include=['strategy_analytics']),
    version='0.0.0.2',
    description='A library to produce strategy analytics',
    author='QuantInsti',
    license='MIT',
)
