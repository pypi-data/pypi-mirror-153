Python package craeted to clean your code up

Current features:
    - Clear console \ Syntax = "clear()"
    - Scrape proxies \ Syntax = "proxy_scrape()"
    - Print slow \ Syntax = "print_slow("Your text")"
    - get headers \ Syntax = "getheaders()"
