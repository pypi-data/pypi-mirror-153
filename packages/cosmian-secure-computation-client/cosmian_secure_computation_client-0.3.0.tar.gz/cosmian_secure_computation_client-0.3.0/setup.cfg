[tool:pytest]
testpaths = tests
addopts = --strict-markers
markers = incremental

[pylint]
disable = C0103,  # invalid-name
	R0801,  # duplicate-code
	R0913,  # too-many-arguments

[pycodestyle]
max-line-length = 90

[egg_info]
tag_build = 
tag_date = 0

